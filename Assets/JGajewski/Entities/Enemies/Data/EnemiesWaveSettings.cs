using UnityEngine;

namespace JGajewski.Entities.Enemies.Data
{
    public class EnemiesWaveSettings : ScriptableObject
    {
        // Start is called before the first frame update
        void Start()
        {
        
        }

        // Update is called once per frame
        void Update()
        {
        
        }
    }
}
